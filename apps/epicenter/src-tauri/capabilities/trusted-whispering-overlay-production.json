{
	"$schema": "../gen/schemas/desktop-schema.json",
	"identifier": "trusted-whispering-overlay-production",
	"description": "Allows the production Whispering recording overlay to exchange its status and action events, and to hand its own window to the OS move loop so the user can drag it. Deliberately not `core:window:default`: it may move itself and nothing more — no sizing, no positioning by coordinate, no reading other windows.",
	"local": false,
	"windows": ["recording-overlay"],
	"remote": {
		"urls": ["http://127.0.0.1:39130"]
	},
	"permissions": [
		"core:event:allow-emit",
		"core:event:allow-listen",
		"core:window:allow-start-dragging",
		"log:default"
	]
}
